flask>=3.0
yfinance>=0.2.30
trafilatura>=1.6
requests>=2.31
PyYAML>=6.0
# Agentic WF mode — install ONE of these for your primary provider:
openai>=1.0       # covers OpenAI, OpenRouter, Perplexity, Gemini via OpenAI-compat API (~5MB)
# anthropic>=0.39 # only if using Anthropic directly (not via OpenRouter)
